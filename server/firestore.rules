rules_version = '2';

// クライアントからの直接アクセスは全拒否する (docs/stock/sync.md)。
// 読み書きの唯一の経路はサーバー (:server) の Admin SDK で、
// Admin SDK はセキュリティルールの対象外。
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
